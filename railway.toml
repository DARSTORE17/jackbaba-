[build]
buildCommand = "npm install && npm run build && composer install --optimize-autoloader --no-dev && php artisan view:clear && php artisan cache:clear"

[deploy]
startCommand = "php artisan serve --host=0.0.0.0 --port=$PORT"
